#include "./adc_readings.h"

int get_battery_percentage(void){
    //get garbage adc readings in for 5s
    //then get next 10 readings and avg
    //return floored int
}

double get_solar_irradiance(){
    //init adc
    //get voltage readings from solar panel
    //get watt readings using current raing
    //divide by solar panel area

    //return solar irradiance
}